name: Build and Release BRP

on:
  push:
    tags:
      - 'brp-srd-v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Build combined RU document
        uses: ./.github/actions/build-markdown
        with:
          source-dir: src/brp/srd-1.0/ru
          output-file: BRP-SRD-1.0-RU.md

      - name: Build combined EN document
        uses: ./.github/actions/build-markdown
        with:
          source-dir: src/brp/srd-1.0/en
          output-file: BRP-SRD-1.0-EN.md

      - name: Install pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-fonts-extra texlive-lang-cyrillic fonts-liberation

      - name: Convert RU to PDF
        uses: ./.github/actions/build-pdf
        with:
          input-file: BRP-SRD-1.0-RU.md
          output-file: BRP-SRD-1.0-RU.pdf
          lang: ru
          toc-title: "Содержание"

      - name: Convert EN to PDF
        uses: ./.github/actions/build-pdf
        with:
          input-file: BRP-SRD-1.0-EN.md
          output-file: BRP-SRD-1.0-EN.pdf
          lang: en
          toc-title: "Table of Contents"

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          name: "Basic Roleplaying SRD — RU + EN ${{ github.ref_name }}"
          body: |
            Basic Roleplaying System Reference Document

            ## SRD 1.0
            - `BRP-SRD-1.0-RU.md` — Markdown (русский)
            - `BRP-SRD-1.0-RU.pdf` — PDF (русский)
            - `BRP-SRD-1.0-EN.md` — Markdown (English)
            - `BRP-SRD-1.0-EN.pdf` — PDF (English)
          files: |
            BRP-SRD-1.0-RU.md
            BRP-SRD-1.0-RU.pdf
            BRP-SRD-1.0-EN.md
            BRP-SRD-1.0-EN.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

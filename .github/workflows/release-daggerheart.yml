name: Build and Release Daggerheart

on:
  push:
    tags:
      - 'dh-srd-v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Build combined RU document
        uses: ./.github/actions/build-markdown
        with:
          source-dir: src/daggerheart/srd-1.0/ru
          output-file: DH-SRD-1.0-RU.md

      - name: Build combined EN document
        uses: ./.github/actions/build-markdown
        with:
          source-dir: src/daggerheart/srd-1.0/en
          output-file: DH-SRD-1.0-EN.md

      - name: Install pandoc and LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-fonts-extra texlive-lang-cyrillic fonts-liberation

      - name: Convert RU to PDF
        uses: ./.github/actions/build-pdf
        with:
          input-file: DH-SRD-1.0-RU.md
          output-file: DH-SRD-1.0-RU.pdf
          lang: ru
          toc-title: "Содержание"

      - name: Convert EN to PDF
        uses: ./.github/actions/build-pdf
        with:
          input-file: DH-SRD-1.0-EN.md
          output-file: DH-SRD-1.0-EN.pdf
          lang: en
          toc-title: "Table of Contents"

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          name: "Daggerheart SRD — RU + EN ${{ github.ref_name }}"
          body: |
            Daggerheart System Reference Document

            ## SRD 1.0
            - `DH-SRD-1.0-RU.md` — Markdown (русский)
            - `DH-SRD-1.0-RU.pdf` — PDF (русский)
            - `DH-SRD-1.0-EN.md` — Markdown (English)
            - `DH-SRD-1.0-EN.pdf` — PDF (English)
          files: |
            DH-SRD-1.0-RU.md
            DH-SRD-1.0-RU.pdf
            DH-SRD-1.0-EN.md
            DH-SRD-1.0-EN.pdf
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

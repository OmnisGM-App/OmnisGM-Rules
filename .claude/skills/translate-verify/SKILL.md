---
description: "Верификация RU глоссария командой из 3 агентов — минимум 3 раунда. Используй после /translate-glossary."
user-invocable: true
---

# /translate-verify — Верификация глоссария

## Использование

```
/translate-verify <game> <version>
```

Пример: `/translate-verify daggerheart srd-1.0`

## Алгоритм

### Шаг 1: Подготовка

1. Найди EN глоссарий: `src/{game}/{version}/en/*_Glossary/`
2. Найди RU глоссарий: `src/{game}/{version}/ru/*_Glossary/`
3. Если RU не найден — предложи сначала `/translate-glossary`
4. **Быстрая проверка скриптом** (ДО запуска агентов):
   ```bash
   python3 .claude/skills/translate-glossary/check_glossary.py \
     src/{game}/{version}/en/*_Glossary/ \
     src/{game}/{version}/ru/*_Glossary/
   ```
   Если скрипт нашёл ошибки — исправь их до запуска агентов. Это экономит раунды.

## Правила проверки

Чеклисты:
- **`.claude/rules/translation-validation.md`** — структура, терминология, оформление, локализация
- **`.claude/rules/translation-quality-review.md`** — редакторская проверка качества (для `verifier-quality`)

Агенты ОБЯЗАНЫ прочитать свои rules файлы перед началом проверки. Для глоссария применяются разделы **1-5** (общие) + раздел **6** (специфика глоссария) + раздел **7** (кросс-версионная консистентность).

### Шаг 2: Запуск верификаторов

Запусти 3 агента **параллельно** — по одному на фокус (structure / consistency / quality), каждый одним вызовом:

```
Agent(subagent_type="general-purpose", description="verify structure", prompt="<промпт агента структуры>", model="opus", run_in_background=true)
```

(×3, по одному вызову на каждый фокус ниже — меняется только `description` и `prompt`; `model` и `run_in_background` одинаковы.)

#### Агент 1: `verifier-structure`

**Фокус:** Структурная целостность

Проверяет разделы **1. Структурное соответствие** + **6. Специфика глоссария → Структура, Точность** из `.claude/rules/translation-validation.md`.

#### Агент 2: `verifier-consistency`

**Фокус:** Консистентность терминологии

Проверяет разделы **2. Терминологическое соответствие** + **6. Специфика глоссария → Консистентность** + **7. Кросс-версионная консистентность** из `.claude/rules/translation-validation.md`.

#### Агент 3: `verifier-quality`

**Фокус:** Качество, оформление и локализация

Проверяет разделы **3. Качество оформления** + **5. Качество локализации** + **6. Специфика глоссария → Оформление** из `.claude/rules/translation-validation.md`.

Дополнительно использует **`.claude/rules/translation-quality-review.md`** как системный промпт — жёсткая редакторская проверка: кальки, канцелярит, тяжеловесность, смысловые смещения.

### Шаг 3: Цикл верификации

Выполни цикл по `.claude/rules/verification-loop.md`.

- **раундов:** 3 (минимум, не ослаблять)
- **режим:** parallel-fanout
- **агенты:** те же 3 Agent-вызова из Шага 2 (structure / consistency / quality)
- **verify-dir:** `src/{game}/{version}/ru/.verify/` — каждый агент пишет находки в `round-{раунд}-{focus}.md` (`focus` = structure/consistency/quality); вызывающая сессия читает только счётчики ошибок и pass/fail
- **при фиксе:** если правка меняет перевод термина → процедура из `.claude/rules/terminology-propagation.md`; коммит на русском «Глоссарий {game} {version}: исправления (раунд {N})»

### Шаг 4: Отчёт

```
Верификация глоссария завершена:
- Раундов: N
- Исправлений: M
- Статус: ✓ Все проверки пройдены

Детали по раундам:
- Раунд 1: 5 ошибок структуры, 3 консистентности, 2 качества → исправлено
- Раунд 2: 1 ошибка консистентности → исправлено
- Раунд 3: 0 ошибок ✓
```

### Шаг 5: Пауза для ручной проверки

Выполни паузу по `.claude/rules/verification-loop.md` (раздел «Пауза ручной проверки»):
путь к файлам — `src/{game}/{version}/ru/*_Glossary/`. При «Внёс правки» — повторить
полный цикл (минимум 3 раунда) и снова показать паузу, пока не «ОК».

## Терминологические решения

Если в ходе верификации обнаружен спорный перевод термина:

1. Прочитай логи в `src/{game}/translate/logs/` и `src/translate/logs/` (общие) — возможно, решение уже задокументировано
2. Если решение уже есть и агент предлагает другой вариант — решение из лога имеет приоритет
3. Если решение новое (термин не в логе) — предложить пользователю и при подтверждении выполнить процедуру из `.claude/rules/terminology-propagation.md`

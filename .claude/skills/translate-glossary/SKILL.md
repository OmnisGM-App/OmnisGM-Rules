---
description: "Перевод EN глоссария в RU — с сохранением оригинальных EN имён. Используй после /build-glossary."
user-invocable: true
---

# /translate-glossary — Перевод глоссария EN→RU

## Использование

```
/translate-glossary <game> <version>
```

Пример: `/translate-glossary daggerheart srd-1.0`

## Алгоритм

### Шаг 1: Инвентаризация EN глоссария и reference-глоссариев

1. Найди директорию глоссария: `src/{game}/{version}/en/*_Glossary/` (Glob)
2. Если не найдена — предложи сначала запустить `/build-glossary`
3. **Построй полный список файлов** для перевода: `src/{game}/{version}/en/{NN}_Glossary/*.md` (Glob)
4. Выведи список пользователю:
   ```
   Файлы EN глоссария для перевода (N файлов):
   - 00_Glossary.md
   - 01_Spells.md
   - 02_Monsters.md
   - ...
   ```
5. **Загрузка словарей `translate/` как reference:**
   - Прочитай `src/translate/01_dictionary_base.md` (общие термины)
   - Прочитай `src/{game}/translate/01_dictionary_base.md` + `02+_dictionary_*.md` (если существуют)
   - Прочитай логи `src/{game}/translate/logs/` и `src/translate/logs/` (решения имеют приоритет)
   - Это **reference-словарь** — приоритетный источник перевода
   - Правила приоритетов: см. `.claude/rules/translate-dictionaries.md`

### Шаг 2: Создание RU директории

1. Определи номер: тот же `{NN}` что у EN глоссария
2. Создай `src/{game}/{version}/ru/{NN}_Glossary/`

### Шаг 3: Перевод `00_Glossary.md` (ПЕРВЫМ — устанавливает терминологию)

Формат RU глоссария: **`.claude/rules/glossary-format.md`**

1. Добавь раздел "Редакционные принципы" (шаблон в rules)
2. Каждая таблица получает колонку "Оригинал" вторым столбцом (формат в rules)
3. Сортировка строк по русскому алфавиту внутри каждой таблицы
4. Числа без изменений
5. Сохрани в `src/{game}/{version}/ru/{NN}_Glossary/00_Glossary.md`
6. **Структурная проверка** — `python3 .claude/skills/translate-glossary/check_glossary.py <en_dir> <ru_dir>`
   - При exit ≠ 0 исправь по выводу скрипта и повтори скрипт (**БЕЗ** opus-агента) до exit 0
7. **Семантическая верификация** — запусти opus-агент через **Agent** (model: "opus"):
   - Агент читает `.claude/rules/translation-validation.md` и проверяет пару EN/RU файлов по чеклисту: терминология по 00_Glossary, естественность RU, соответствие reference-словарю — **БЕЗ** структурных пунктов (их уже покрыл скрипт)
8. **Fix-цикл семантики:** если агент нашёл ошибки → исправить → перезапустить **только opus-шаг** → повторять до 0 ошибок
   - Если исправление меняет перевод термина → выполнить процедуру из `.claude/rules/terminology-propagation.md`
9. **Коммит** (только после чистой верификации): `Глоссарий RU {game} {version}: перевод 00_Glossary.md`

### Шаг 4: Перевод остальных файлов (01+) — ОБЯЗАТЕЛЬНО ВСЕ

**Переведи КАЖДЫЙ файл** из списка, составленного на шаге 1 (кроме `00_Glossary.md`, который уже переведён на шаге 3). Пропуск файлов запрещён.

Для каждого файла (в порядке номеров):

1. Читай EN файл
2. Переводи, **строго следуя терминологии из 00_Glossary.md**
3. Формат таблиц — по `.claude/rules/glossary-format.md` (RU имя → Оригинал → остальные колонки)
4. **Сортировка по русскому алфавиту** (по первой колонке, **ё = е** при сортировке — "Тёмное зрение" стоит рядом с "Телепатия", а не после "Труднопроходимая")
5. Числа без изменений (уровни, КО, размеры)
6. Сохрани в `src/{game}/{version}/ru/{NN}_Glossary/{filename}`
7. **Структурная проверка** — `python3 .claude/skills/translate-glossary/check_glossary.py <en_dir> <ru_dir>`
   - При exit ≠ 0 исправь по выводу скрипта и повтори скрипт (**БЕЗ** opus-агента) до exit 0
8. **Семантическая верификация** — запусти opus-агент через **Agent** (model: "opus"):
   - Агент читает `.claude/rules/translation-validation.md` и проверяет пару EN/RU файлов по чеклисту: терминология по 00_Glossary, естественность RU, соответствие reference-словарю — **БЕЗ** структурных пунктов (их уже покрыл скрипт)
9. **Fix-цикл семантики:** если агент нашёл ошибки → исправить → перезапустить **только opus-шаг** → повторять до 0 ошибок
   - Если исправление меняет перевод термина → выполнить процедуру из `.claude/rules/terminology-propagation.md`
10. **Коммит** (только после чистой верификации): `Глоссарий RU {game} {version}: перевод {filename}`

### Шаг 5: Обновление словарей `translate/`

После перевода всех файлов глоссария — создай/обнови словари в `src/{game}/translate/`:

1. Если `src/{game}/translate/` не существует — создай
2. Если `logs/` не существует — создай директорию
3. Создай/обнови `01_dictionary_base.md` из `00_Glossary.md` (базовые термины)
   - Включи **расширенные категории** (см. `.claude/rules/translate-dictionaries.md`):
     - Названия разделов/глав (сканируй `#` заголовки всех EN файлов)
     - Названия подклассов (из файлов классов)
     - Названия ключевых умений классов (из файлов классов)
     - Рекуррентные фразы (Using a Higher-Level Spell Slot и т.п.)
     - Единицы и сокращения (ft., GP, SP, CP)
4. Создай/обнови `02+_dictionary_*.md` из `01+` файлов глоссария (сущности)
5. Формат словарей — по `.claude/rules/translate-dictionaries.md`
6. **Коммит:** `Словари {game}: создание/обновление из глоссария {version}`

### Шаг 6: Проверка полноты и итоговый отчёт

1. **Финальная структурная проверка** — однократно запусти скрипт по всему глоссарию:
   ```bash
   python3 .claude/skills/translate-glossary/check_glossary.py \
     src/{game}/{version}/en/{NN}_Glossary/ \
     src/{game}/{version}/ru/{NN}_Glossary/
   ```
   Скрипт сам печатает расхождение количества файлов (и какие пропущены/лишние), строк в таблицах, отсутствие колонки "Оригинал", нарушение сортировки (ё=е) и счётчики по каждому файлу.
2. Если exit ≠ 0 — исправь по выводу (пропущенные файлы переведи через шаг 4) и повтори скрипт до exit 0
3. Выведи итоговый отчёт (счётчики бери из вывода скрипта):

```
Глоссарий RU создан (N из N файлов):
- 00_Glossary.md: N категорий, M терминов ✓
- 01_Spells.md: K заклинаний (EN: K) ✓
- 02_Monsters.md: L монстров (EN: L) ✓
...
Словари translate/ обновлены: M файлов
Все файлы переведены. Все количества совпадают с EN.
```

## Принципы перевода

- **Reference-словарь — приоритет.** Если термин уже переведён в другой версии той же системы → использовать тот же перевод
- **Один EN термин = один RU перевод** — консистентность во всём глоссарии и между версиями
- Устоявшиеся термины D&D/TTRPG → использовать общепринятые переводы
- Новые/уникальные термины → перевод по смыслу
- Имена собственные → транслитерация
- Игромеханические термины → с заглавной буквы
- Осознанное отклонение от reference-словаря допустимо, но требует обоснования и подтверждения пользователем

## Терминологические решения

При спорном выборе перевода (несколько допустимых вариантов, отклонение от reference, нестандартный термин):

1. Прочитай логи в `src/{game}/translate/logs/` и `src/translate/logs/` (общие) — возможно, решение уже принято
2. Если решение новое — после подтверждения пользователем **добавь запись** в `src/{game}/translate/logs/{date}_log.md`
3. Формат: `### EN термин → RU перевод`, поля: Решение, Причина, Отвергнуто

## Скрипты

| Скрипт | Что делает |
|--------|------------|
| `check_glossary.py` | Проверка пар EN/RU: файлы, строки, колонка "Оригинал", сортировка ё=е |

## Технические требования

- Все агенты — **model: "opus"**
- Большие файлы (>3000 строк) — чанками
- RU файл всегда содержит колонку "Оригинал"
- **Сортировка: ё = е** (Тёмное → рядом с Телепатия, не после Труднопроходимая)
- Коммит после каждого файла
- Сообщения коммитов на русском

---
description: "Верификация полноты импорта — сверка markdown с оригинальным PDF, циклическая. Используй после /cleanup-artifacts."
user-invocable: true
---

# /verify-import — Верификация полноты импорта

## Использование

```
/verify-import <game> <version>
```

Пример: `/verify-import brp srd-1.0`

## Правила проверки

Чеклист: **`.claude/rules/verify-import.md`** — классификация проблем, формат отчёта, hard failure rule.

Агент ОБЯЗАН прочитать этот файл перед началом проверки.

## Предварительные условия

- EN файлы: `src/{game}/{version}/en/**/*.md`
- Оригинальный PDF для сверки (спросить путь если неизвестен)

## Алгоритм

### Шаг 1: Автоматические проверки (скрипты)

Запусти скрипты ДО агента:

```bash
# Структурная проверка markdown (таблицы, заголовки, bold)
python3 .claude/skills/verify-import/check_markdown.py src/{game}/{version}/en/
```

Если скрипт нашёл ошибки — исправь их до запуска агента.

### Шаг 2: Подготовка

1. Найди все EN файлы: `src/{game}/{version}/en/**/*.md`
2. Если файлов нет — предложи `/convert-pdf` + `/cleanup-artifacts`
3. Определи путь к оригинальному PDF

### Шаг 3: Верификация (один агент, verify+fix внутри)

Выполни цикл по `.claude/rules/verification-loop.md`.

- **раундов:** 2 чистых (минимум, не ослаблять)
- **режим:** verify+fix-in-agent
- **агент:** один opus-агент — проверяет по A-F, правит, гоняет `check_markdown.py` (branch на exit code), коммитит «Импорт {game} {version}: верификация (раунд {N})» внутри своего контекста
- **сверка полноты (A):** обязательно с PDF — первые 5 страниц, середина (3 случайные), последние 5 страниц
- **возврат:** один финальный статус `✓/✗, N раундов, M правок`

### Критерии проверки (A-F)

`A` Полнота · `B` Структура · `C` Таблицы · `D` Списки · `E` Текст · `F` Форматирование

Полные описания — в чеклисте из раздела «Правила проверки».

Проверки `B` (стартовый `#`, иерархия заголовков), `C` (согласованность столбцов) и
`F` (баланс `**`/`*`) уже сделаны `check_markdown.py` в Шаге 1 — **не перепроверять**.
Агент измеряет только то, чего скрипт не делает: `A` полнота vs PDF, `D` число
элементов списков vs PDF, `E` OCR-мусор, целостность blockquotes.

### Шаг 4: Пауза для ручной проверки

```
Импорт завершён и верифицирован (N раундов, M исправлений).

Файлы: src/{game}/{version}/en/
- 00_Legal.md
- 01_....md
...

Проверьте файлы. Всё ОК или нужны правки?
```

### Шаг 5: Итоговый отчёт

```
Верификация импорта завершена:
- Файлов проверено: K
- Раундов: N
- Исправлений: M
- Статус: ✓ / ✗
```

## Скрипты

| Скрипт | Что делает |
|--------|------------|
| `check_markdown.py` | Автоматические проверки: таблицы, заголовки, bold/italic, пустые файлы |

## Технические требования

- Агент — **model: "opus"**
- Большие файлы (>3000 строк) — чтение чанками
- PDF читается через Read tool (параметр `pages`)
- Коммит только при реальных исправлениях
- Минимум 2 раунда (1 проверка + 1 контрольный)
- Сообщения коммитов на русском
